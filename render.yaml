# Render blueprint — deploys Hotpath as a static site.
# Minimal on purpose: extra headers/routes blocks can break asset serving.
services:
  - type: web
    name: hotpath
    runtime: static
    buildCommand: echo "static site, nothing to build"
    staticPublishPath: ./
